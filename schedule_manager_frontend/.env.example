# Frontend configuration for Schedule Manager (do not commit real .env with secrets)
# The orchestrator will set these in the deployment environment.

# Base URL for REST/GraphQL API (e.g., https://api.example.com)
REACT_APP_API_BASE=/api

# Relative path for GraphQL endpoint if used
REACT_APP_GRAPHQL_PATH=/graphql

# WebSocket URL for live updates (e.g., wss://api.example.com/ws)
REACT_APP_WS_URL=
